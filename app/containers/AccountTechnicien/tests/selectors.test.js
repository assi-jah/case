// import { selectAccountTechnicienDomain } from '../selectors';

describe('selectAccountTechnicienDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
