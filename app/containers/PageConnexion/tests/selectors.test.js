// import { selectPageConnexionDomain } from '../selectors';

describe('selectPageConnexionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
