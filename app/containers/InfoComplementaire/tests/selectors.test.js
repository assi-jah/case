// import { selectInfoComplementaireDomain } from '../selectors';

describe('selectInfoComplementaireDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
