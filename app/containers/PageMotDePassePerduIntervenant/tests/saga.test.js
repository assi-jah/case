/**
 * Test sagas
 */

/* eslint-disable redux-saga/yield-effects */
// import { take, call, put, select } from 'redux-saga/effects';
// import pageMotDePassePerduIntervenantSaga from '../saga';

// const generator = pageMotDePassePerduIntervenantSaga();

describe('pageMotDePassePerduIntervenantSaga Saga', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
