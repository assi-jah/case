// import { selectPageMotDePassePerduIntervenantDomain } from '../selectors';

describe('selectPageMotDePassePerduIntervenantDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
