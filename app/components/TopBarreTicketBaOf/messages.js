/*
 * TopBarreTicketBaOf Messages
 *
 * This contains all the text for the TopBarreTicketBaOf component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TopBarreTicketBaOf';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TopBarreTicketBaOf component!',
  },
});
